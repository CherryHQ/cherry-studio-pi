/** Tools disabled for ALL agents. Keep empty so agents can use provider/runtime web tools when available. */
export const GLOBALLY_DISALLOWED_TOOLS = [] as const

/** Tools disabled when Soul Mode is active. Keep empty so Soul Mode does not hide runtime capabilities. */
export const SOUL_MODE_DISALLOWED_TOOLS = [] as const
